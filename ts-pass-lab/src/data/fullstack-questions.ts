import type { Question, QuestionDifficulty } from '@/types/platform';

type Topic = {
  id: string;
  category: string;
  concept: string;
  correct: string;
  distractors: string[];
  code: string;
  output: string;
  errorCode: string;
  errorReason: string;
  fillCode: string;
  fillAnswer: string;
  examPoint: string;
  difficulty: QuestionDifficulty;
};

const topics: Topic[] = [
  {id:'fs-http',category:'HTTP',concept:'新しいリソースを作成する代表的なHTTPメソッドは？',correct:'POST',distractors:['GET','DELETE','HEAD'],code:'const status = 201;\nconsole.log(status >= 200 && status < 300 ? "success" : "error");',output:'success',errorCode:'return Response.json({ created: true }, { status: 200 });',errorReason:'作成成功なら201を使う方が意図を表しやすい',fillCode:'_____ /api/tasks',fillAnswer:'POST',examPoint:'メソッドとステータスコードで処理結果を一貫して表します。',difficulty:'basic'},
  {id:'fs-html',category:'HTML',concept:'ユーザーが押して操作する要素として最も適切なのは？',correct:'button',distractors:['div','span','meta'],code:'const tags = ["header","main","footer"];\nconsole.log(tags.join("-"));',output:'header-main-footer',errorCode:'<div onClick={save}>保存</div>',errorReason:'操作にはbuttonを使う方がキーボード操作や意味を保てる',fillCode:'<_____ type="button">保存</_____>',fillAnswer:'button',examPoint:'見た目ではなく要素の意味と標準操作を利用します。',difficulty:'basic'},
  {id:'fs-css',category:'CSS',concept:'2次元の行と列を扱うレイアウトに向くCSSは？',correct:'Grid',distractors:['float only','font-face','z-index'],code:'const width = 390;\nconsole.log(width < 700 ? 1 : 3);',output:'1',errorCode:'.card { width: 900px; }',errorReason:'固定幅だけでは小さい画面で横にはみ出しやすい',fillCode:'grid-template-columns: repeat(auto-fit, _____(240px, 1fr));',fillAnswer:'minmax',examPoint:'モバイル幅と内容の最小幅を確認します。',difficulty:'basic'},
  {id:'fs-react-props',category:'React',concept:'Reactで親から子へ渡す入力は？',correct:'Props',distractors:['Migration','Cookie','Index'],code:'type Props={count?:number};\nconst view=(p:Props)=>p.count ?? 0;\nconsole.log(view({}));',output:'0',errorCode:'type Props={title:string};\nview({});',errorReason:'必須Propsのtitleが渡されていない',fillCode:'type _____ = { title: string };',fillAnswer:'Props',examPoint:'Propsは外部入力、Stateは内部で変化する値です。',difficulty:'basic'},
  {id:'fs-react-state',category:'React State',concept:'ReactのStateを更新するときの基本は？',correct:'新しい値を作ってsetterへ渡す',distractors:['配列を直接pushする','DOMを直接書き換える','変数だけ変更する'],code:'const state={count:2};\nconst next={...state,count:state.count+1};\nconsole.log(next.count);',output:'3',errorCode:'items.push(newItem); setItems(items);',errorReason:'同じ配列を直接変更すると更新を正しく検知しにくい',fillCode:'setItems(_____items, newItem]);',fillAnswer:'[...',examPoint:'Stateは直接変更せず新しい参照を作ります。',difficulty:'standard'},
  {id:'fs-next-router',category:'Next.js',concept:'App Routerで/products/42のような動的部分を表すフォルダ名は？',correct:'[id]',distractors:['{id}','<id>','id?'],code:'const path=(id:string)=>`/products/${id}`;\nconsole.log(path("42"));',output:'/products/42',errorCode:'app/products/id/page.tsx',errorReason:'この構成ではidという固定URLになり動的値を受け取れない',fillCode:'app/products/_____/page.tsx',fillAnswer:'[id]',examPoint:'フォルダ構造とURLの対応を読み取ります。',difficulty:'standard'},
  {id:'fs-server-client',category:'Server/Client',concept:'useStateやクリックイベントが必要なコンポーネントは？',correct:'Client Component',distractors:['SQL View','Migration','Server-only module'],code:'type Side="server"|"client";\nconst interactive:Side="client";\nconsole.log(interactive);',output:'client',errorCode:'// Server Component\nconst [open,setOpen]=useState(false);',errorReason:'状態Hookを使う境界にはuse clientが必要',fillCode:'"_____";\nimport { useState } from "react";',fillAnswer:'use client',examPoint:'対話機能だけをClient境界にして送信JavaScriptを抑えます。',difficulty:'standard'},
  {id:'fs-route-handler',category:'Route Handler',concept:'Next.js Route HandlerでPOSTを処理する関数名は？',correct:'POST',distractors:['postHandler','CREATE','submit'],code:'const ok=true;\nconsole.log(ok ? 201 : 400);',output:'201',errorCode:'export function create(request: Request) {}',errorReason:'Route HandlerはGETやPOSTなどHTTPメソッド名をexportする',fillCode:'export async function _____(request: Request) {}',fillAnswer:'POST',examPoint:'Requestを読み、検証し、適切なResponseを返します。',difficulty:'standard'},
  {id:'fs-rest',category:'REST API',concept:'既存リソースの一部更新に使われる代表的メソッドは？',correct:'PATCH',distractors:['TRACE','CONNECT','HEAD'],code:'const methods={create:"POST",update:"PATCH"};\nconsole.log(methods.update);',output:'PATCH',errorCode:'POST /api/updateTask',errorReason:'リソースURLとPATCHを組み合わせる方が一貫したREST設計になる',fillCode:'_____ /api/tasks/123',fillAnswer:'PATCH',examPoint:'URLはリソース、メソッドは操作を表します。',difficulty:'standard'},
  {id:'fs-validation',category:'バリデーション',concept:'request.json()の結果を安全に受ける最初の型は？',correct:'unknown',distractors:['any固定','never','HTMLElement'],code:'const value:unknown={title:"API"};\nconsole.log(typeof value === "object");',output:'true',errorCode:'const body = await request.json() as TaskInput;\nawait save(body);',errorReason:'型アサーションだけでは実際の入力内容を検証できない',fillCode:'const body: _____ = await request.json();',fillAnswer:'unknown',examPoint:'クライアント入力はサーバーで実行時検証します。',difficulty:'standard'},
  {id:'fs-status',category:'APIエラー',concept:'認証は済んでいるが権限がない場合の代表的ステータスは？',correct:'403',distractors:['201','204','301'],code:'const owner=false;\nconsole.log(owner ? 200 : 403);',output:'403',errorCode:'if (!owner) return Response.json({error:"forbidden"},{status:500});',errorReason:'予期しないサーバー障害ではなく権限不足なので403が適切',fillCode:'return new Response(null, { status: _____ }); // forbidden',fillAnswer:'403',examPoint:'400、401、403、404、500の意味を区別します。',difficulty:'standard'},
  {id:'fs-sql',category:'SQL',concept:'データを読み取るSQL命令は？',correct:'SELECT',distractors:['INSERT','DROP','COMMIT'],code:'const rows=[{done:false},{done:true}];\nconsole.log(rows.filter(r=>!r.done).length);',output:'1',errorCode:'DELETE FROM tasks;',errorReason:'WHEREがないため全行削除になる',fillCode:'_____ id, title FROM tasks WHERE completed = false;',fillAnswer:'SELECT',examPoint:'更新・削除では対象条件を必ず確認します。',difficulty:'basic'},
  {id:'fs-relations',category:'DB設計',concept:'テーブル内の各行を一意に識別する列は？',correct:'主キー',distractors:['CSSクラス','HTTPヘッダー','Cookie'],code:'const posts=[{id:1,userId:7},{id:2,userId:8}];\nconsole.log(posts.filter(p=>p.userId===7).length);',output:'1',errorCode:'posts.userName -> users.name',errorReason:'変更され得る名前ではなく安定したuserIdで関連付ける',fillCode:'posts.user_id -> users._____',fillAnswer:'id',examPoint:'主キーと外部キーでリレーションを表します。',difficulty:'standard'},
  {id:'fs-prisma',category:'Prisma',concept:'Prismaスキーマ変更をDBへ履歴付きで反映するものは？',correct:'Migration',distractors:['Props','useEffect','CORS'],code:'const input={title:"DB"};\nconst task={id:"t1",completed:false,...input};\nconsole.log(task.completed);',output:'false',errorCode:'本番DBだけ手作業で列を追加する',errorReason:'スキーマとmigration履歴がコードと一致しなくなる',fillCode:'await prisma.task._____({ data: { title } });',fillAnswer:'create',examPoint:'schema、migration、生成クライアントの整合性を保ちます。',difficulty:'standard'},
  {id:'fs-transaction',category:'Transaction',concept:'複数DB更新をまとめて成功または失敗させる仕組みは？',correct:'トランザクション',distractors:['メディアクエリ','Props','CDN'],code:'const a={balance:100};const b={balance:0};\nconst amount=30;\nconsole.log(b.balance+amount);',output:'30',errorCode:'残高を減らして確定後、増額処理だけ失敗する',errorReason:'2つの更新を同じトランザクションに入れる必要がある',fillCode:'BEGIN; UPDATE ...; _____;',fillAnswer:'COMMIT',examPoint:'途中失敗で不整合が起きる処理をまとめます。',difficulty:'advanced'},
  {id:'fs-session',category:'認証',concept:'JavaScriptから読めないCookie属性は？',correct:'HttpOnly',distractors:['display','grid','readonly'],code:'const expiresAt=200;\nconst now=100;\nconsole.log(expiresAt>now);',output:'true',errorCode:'localStorage.setItem("password", password);',errorReason:'パスワードや長期秘密情報をブラウザへ平文保存してはいけない',fillCode:'Set-Cookie: session=abc; _____; Secure',fillAnswer:'HttpOnly',examPoint:'Cookie属性、有効期限、署名検証を確認します。',difficulty:'advanced'},
  {id:'fs-authorization',category:'認可',concept:'「ログイン中の利用者がこのデータを操作してよいか」の確認は？',correct:'認可',distractors:['認証だけ','型推論','キャッシュ'],code:'const canDelete=(role:string,owner:boolean)=>role==="admin"||owner;\nconsole.log(canDelete("user",false));',output:'false',errorCode:'if (session) deleteTask(taskId);',errorReason:'ログイン確認だけで対象データの所有者や権限を確認していない',fillCode:'if (task.userId !== session.user.id) return _____;',fillAnswer:'403',examPoint:'認証と認可を別々に確認します。',difficulty:'advanced'},
  {id:'fs-security',category:'セキュリティ',concept:'SQL Injectionを防ぐ基本は？',correct:'パラメータ化クエリ',distractors:['文字列結合','CORSを*にする','CSSを削除する'],code:'const allowed=["https://app.example.com"];\nconsole.log(allowed.includes("https://evil.example"));',output:'false',errorCode:'`SELECT * FROM users WHERE name = "${name}"`',errorReason:'外部入力をSQL文字列へ直接結合している',fillCode:'WHERE email = _____ // parameter',fillAnswer:'$1',examPoint:'入力をHTML・SQLへ直接結合しません。',difficulty:'advanced'},
  {id:'fs-testing',category:'テスト',concept:'5000円以上で送料無料の境界値として確認すべき組は？',correct:'4999円と5000円',distractors:['100円だけ','10000円だけ','文字列だけ'],code:'const fee=(amount:number)=>amount>=5000?0:500;\nconsole.log([4999,5000].map(fee).join(","));',output:'500,0',errorCode:'test("fee",()=>expect(fee(1000)).toBe(500));',errorReason:'正常系1件だけでは境界条件の不具合を見つけにくい',fillCode:'expect(fee(_____)).toBe(0);',fillAnswer:'5000',examPoint:'正常系、境界値、異常系を分けて確認します。',difficulty:'standard'},
  {id:'fs-deploy',category:'デプロイ',concept:'デプロイ直後に公開環境で主要機能を短く確認するテストは？',correct:'スモークテスト',distractors:['型アサーション','正規化','Props drilling'],code:'const checks=[true,true,true];\nconsole.log(checks.every(Boolean));',output:'true',errorCode:'build成功だけで公開完了と判断する',errorReason:'本番URL、API、DB接続、ログも確認する必要がある',fillCode:'build -> deploy -> _____ test -> monitor',fillAnswer:'smoke',examPoint:'公開後の稼働確認とロールバック方法を準備します。',difficulty:'advanced'},
];

const rotate = <T,>(items:T[],amount:number)=>items.map((_,index)=>items[(index+amount)%items.length]);

export const fullstackQuestions: Question[] = topics.flatMap((topic,index)=>{
  const conceptChoices=rotate([topic.correct,...topic.distractors],index%4);
  const outputChoices=rotate([topic.output,'型エラー','undefined','何も表示されない'],(index+1)%4);
  const errorChoices=rotate([topic.errorReason,'問題はない','セミコロンだけが原因','CSSの問題'],(index+2)%4);
  const fillChoices=rotate([topic.fillAnswer,'any','void','never'],(index+3)%4);
  return [
    {id:`${topic.id}-concept`,category:topic.category,difficulty:topic.difficulty,type:'choice' as const,question:topic.concept,choices:conceptChoices,answer:conceptChoices.indexOf(topic.correct),explanation:`正解は「${topic.correct}」です。${topic.examPoint}`,wrongReasons:conceptChoices.map((choice)=>choice===topic.correct?'正しい選択肢です。':`「${choice}」はこの目的に合いません。`),examPoint:topic.examPoint},
    {id:`${topic.id}-output`,category:topic.category,difficulty:topic.difficulty,type:'output' as const,question:'次のコードの実行結果を選んでください。',code:topic.code,choices:outputChoices,answer:outputChoices.indexOf(topic.output),explanation:`上から処理を追うと「${topic.output}」になります。`,wrongReasons:outputChoices.map((choice)=>choice===topic.output?'正しい出力です。':`このコードでは「${choice}」になりません。`),examPoint:topic.examPoint},
    {id:`${topic.id}-reading`,category:topic.category,difficulty:topic.difficulty,type:'reading' as const,question:'次の実装で最も重要な問題はどれですか？',code:topic.errorCode,choices:errorChoices,answer:errorChoices.indexOf(topic.errorReason),explanation:topic.errorReason,wrongReasons:errorChoices.map((choice)=>choice===topic.errorReason?'主な問題点です。':`「${choice}」は主原因ではありません。`),examPoint:topic.examPoint},
    {id:`${topic.id}-fill`,category:topic.category,difficulty:topic.difficulty,type:'fill' as const,question:'空欄へ入る最も適切な語を選んでください。',code:topic.fillCode,choices:fillChoices,answer:fillChoices.indexOf(topic.fillAnswer),explanation:`空欄には「${topic.fillAnswer}」が入ります。`,wrongReasons:fillChoices.map((choice)=>choice===topic.fillAnswer?'目的と構文に合っています。':`「${choice}」では意図した処理になりません。`),examPoint:topic.examPoint},
    {id:`${topic.id}-true-false`,category:topic.category,difficulty:topic.difficulty,type:'trueFalse' as const,question:`「${topic.examPoint}」この説明は正しいですか？`,choices:['正しい','誤り'],answer:0,explanation:topic.examPoint,wrongReasons:['説明どおりで正しいです。','この説明はWeb開発の基本原則に沿っています。'],examPoint:topic.examPoint},
  ];
});
